- data = site.data.header
header
    .header-wrap
        .logo-group
            a.logo-link(href=url_for())
                img(src=url_for(theme.logo) alt="logo")
            span.conf-date
                !=data["date"]
        .header-right
            .navigation
                include nav-main
                include nav-sub
                include nav-mobile
            include social-bar
            a.button.buy-tickets(href=data["register-link"]) Register
